source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in paypal_client.gemspec
gemspec

group :development, :test do
  gem 'pry'
  gem 'rubocop'
  gem 'simplecov'
  gem 'yard'
end
